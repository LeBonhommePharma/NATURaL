# visionOS — spatial session

Overrides MASTER for `BonhommeVision/`.

- **Window:** plan browser + active pose + End. Trailing ornament is biofeedback (`SessionHUDMetrics` / CompactSCI + progress), not a second window of cyan gauges.
- **Immersive:** mixed `ImmersiveSpace` for 3D pose; toggle is labeled Open 3D / Close 3D (SF Symbol `cube`).
- **Color:** mint CTA, violet SCI, strawberry pause — same tokens as phone. No `.cyan` / `.green` / `.orange` SCI bands.
- **Hits:** ornament width ~180–200pt; End is a labeled button, not an unlabeled x.
- **Do not:** HealthKit recording on Vision; Exergy remaining-dial; emoji.
